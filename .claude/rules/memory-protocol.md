# Memory Protocol

After completing any agent task:
1. Log the action to the agent's `memory/YYYY-MM-DD.md` (create if needed)
2. If a recurring pattern is discovered, update the agent's `MEMORY.md`
3. If the learning affects other agents, also update `shared/MEMORY.md`
